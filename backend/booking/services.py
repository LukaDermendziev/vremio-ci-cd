"""
Domain services for booking availability and prepared messages.

The first implementation will calculate slots dynamically from working hours,
date overrides, unavailable blocks, active bookings, service duration, slot
interval, and buffer time.
"""

